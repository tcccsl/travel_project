// Import components from uni-ui as needed
// For uni-app, we need to use easycom pattern instead of direct imports
export default {
  install(app) {
    // With easycom in pages.json, components will be automatically registered
    // No need to manually register components here
  }
} 